# TODO(you): apply a default-deny in namespace records — TASKS.md §4
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: records
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
