# SOPS creation rules — encrypt Kubernetes Secret manifests with age.
#
# TODO(you): generate an age keypair, put the public key below, keep the private
# key in local/age/ (gitignored). See TASKS.md §3.
creation_rules:
  - path_regex: secrets/.*\.yaml$
    encrypted_regex: ^(data|stringData)$
    # Replace with: age1...
    age: age1xzzuwhkv7qed8cx4tchalznj5m3glxfalhjg52l8wju7gr47vaxqft856a
