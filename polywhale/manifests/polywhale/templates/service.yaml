apiVersion: v1
kind: Service
metadata:
  name: {{ include "polywhale.fullname" . }}
  labels:
    {{- include "polywhale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "polywhale.selectorLabels" . | nindent 4 }}
