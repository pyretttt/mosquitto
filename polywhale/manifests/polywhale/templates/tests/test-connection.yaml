apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "polywhale.fullname" . }}-test-connection"
  labels:
    {{- include "polywhale.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "polywhale.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
