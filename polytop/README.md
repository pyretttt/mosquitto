# Polytop

Create Golang bubble tea tui application scaffold to monitor polymarket. Inside `go` folder

Create Rust ratutui + tokio tui appication scaffold to monitor polymarket. Inside `rust` folder

Create Python textual tui application scaffold to monitor polymarket. Inside `py` folder

In both case make just an tui that shows hello world