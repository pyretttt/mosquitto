{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "cmake_build_root",
            "command": "cmake",
            "args": [
                "--build",
                "./build",
                "-j",
                "2"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Runs cmake build"
        }
    ]
}