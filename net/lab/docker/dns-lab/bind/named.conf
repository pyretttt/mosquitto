options {
    directory "/var/cache/bind";
    listen-on { any; };
    allow-query { any; };
    recursion no;
    dnssec-validation no;
};

zone "lab.local" {
    type master;
    file "/etc/bind/db.lab.local";
};
